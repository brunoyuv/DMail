#!/usr/bin/env bash
# Compatibility entry point: picture consent/refresh is superseded by snapshots.
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec "$ROOT/scripts/test-static-picture-snapshot" "$@"

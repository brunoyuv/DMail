export function equation(source: string, display: boolean): string;
export function markdown(source: string): string;
export function typesetEquation(source: string, display: boolean, renderer: string): string;
export function mathStyles(renderer: string): string;

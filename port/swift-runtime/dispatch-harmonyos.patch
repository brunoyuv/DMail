--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -7,6 +7,10 @@
 # CMAKE_MSVC_RUNTIME_LIBRARY.  Requires CMake 3.15 or newer.
 if(POLICY CMP0091)
   cmake_policy(SET CMP0091 NEW)
+endif()
+
+if(POLICY CMP0157)
+  cmake_policy(SET CMP0157 NEW)
 endif()
 
 project(dispatch
--- a/src/shims/getprogname.h
+++ b/src/shims/getprogname.h
@@ -26,7 +26,7 @@
 
 #if !HAVE_GETPROGNAME
 
-#ifdef __ANDROID__
+#if defined(__ANDROID__) || defined(__OHOS__)
 extern const char *__progname;
 #endif /* __ANDROID */
 
@@ -34,12 +34,12 @@
 const char *getprogname(void);
 #else
 
-static inline char *
+static inline const char *
 getprogname(void)
 {
 # if HAVE_DECL_PROGRAM_INVOCATION_SHORT_NAME
 	return program_invocation_short_name;
-# elif defined(__ANDROID__)
+# elif defined(__ANDROID__) || defined(__OHOS__)
 	return __progname;
 # else
 #   error getprogname(3) is not available on this platform
--- a/src/swift/CMakeLists.txt
+++ b/src/swift/CMakeLists.txt
@@ -1,3 +1,11 @@
+if(CMAKE_SYSTEM_NAME STREQUAL "OHOS")
+  set(Dispatch_SWIFT_MODULEMAP "${CMAKE_INSTALL_PREFIX}/lib/swift/dispatch/module.modulemap")
+  set(Dispatch_SWIFT_HEADER_ROOT "${CMAKE_INSTALL_PREFIX}/lib/swift")
+else()
+  set(Dispatch_SWIFT_MODULEMAP "${PROJECT_SOURCE_DIR}/dispatch/module.modulemap")
+  set(Dispatch_SWIFT_HEADER_ROOT "${PROJECT_SOURCE_DIR}")
+endif()
+
 if(HAVE_OBJC)
   add_library(DispatchStubs STATIC
     DispatchStubs.m)
@@ -17,11 +25,11 @@
   Wrapper.swift)
 target_compile_options(swiftDispatch PRIVATE
   "SHELL:-Xcc -fblocks"
-  "SHELL:-Xcc -fmodule-map-file=${PROJECT_SOURCE_DIR}/dispatch/module.modulemap"
-  "SHELL:-Xcc -I${PROJECT_SOURCE_DIR}"
+  "SHELL:-Xcc -fmodule-map-file=${Dispatch_SWIFT_MODULEMAP}"
+  "SHELL:-Xcc -I${Dispatch_SWIFT_HEADER_ROOT}"
   "SHELL:-Xcc -I${PROJECT_SOURCE_DIR}/src/swift/shims")
 target_compile_options(swiftDispatch PUBLIC
-  "SHELL:-vfsoverlay ${CMAKE_BINARY_DIR}/dispatch-vfs-overlay.yaml")
+  "$<$<NOT:$<PLATFORM_ID:OHOS>>:SHELL:-vfsoverlay ${CMAKE_BINARY_DIR}/dispatch-vfs-overlay.yaml>")
 set_target_properties(swiftDispatch PROPERTIES
   Swift_MODULE_NAME Dispatch
   Swift_MODULE_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR}/swift
@@ -32,7 +40,7 @@
 target_link_libraries(swiftDispatch PUBLIC
   dispatch)
 if(NOT CMAKE_SYSTEM_NAME MATCHES "Darwin|Windows")
-  target_link_options(swiftDispatch PRIVATE "SHELL:-no-toolchain-stdlib-rpath")
+  target_link_options(swiftDispatch PRIVATE "$<$<LINK_LANGUAGE:Swift>:-no-toolchain-stdlib-rpath>")
   set_target_properties(swiftDispatch PROPERTIES INSTALL_RPATH "$ORIGIN")
 endif()
 
@@ -53,3 +61,9 @@
     EXPORT dispatchExports
     DESTINATION ${INSTALL_TARGET_DIR})
 endif()
+
+if(CMAKE_SYSTEM_NAME STREQUAL "OHOS")
+  find_package(SwiftCore CONFIG REQUIRED)
+  set_target_properties(swiftDispatch PROPERTIES LINKER_LANGUAGE CXX)
+  target_link_libraries(swiftDispatch PRIVATE swiftCore swift_Concurrency)
+endif()
